<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Agendar evento</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background-color: #f8f9fa; }
    .card { max-width: 700px; margin: 2rem auto; }

    .calendar-grid {
      display: grid;
      grid-template-columns: repeat(7, 1fr);
      gap: 4px;
      margin-top: 8px;
    }
    .calendar-grid .day-cell {
      width: 100%;
      aspect-ratio: 1/1;
      display: flex;
      align-items: center;
      justify-content: center;
      border-radius: 50%;
      cursor: pointer;
      transition: all 0.2s;
      font-size: 0.9rem;
      background-color: #e9ecef;
      user-select: none;
    }
    .calendar-grid .day-cell:hover:not(.disabled) { background-color: #dee2e6; }
    .calendar-grid .day-cell.selected { background-color: #0d6efd; color: #fff; }
    .calendar-grid .day-cell.has-availability { background-color: #d1e7dd; }
    .calendar-grid .day-cell.has-availability.selected { background-color: #0d6efd; color: #fff; }
    .calendar-grid .day-cell.disabled { opacity: 0.4; cursor: not-allowed; }

    .month-nav { cursor: pointer; user-select: none; font-size: 1.2rem; }
    .month-nav:hover { color: #0d6efd; }

    .block-btn {
      margin: 4px;
      min-width: 80px;
    }
    .block-btn.selected {
      background-color: #0d6efd;
      color: #fff;
      border-color: #0d6efd;
    }
    .block-btn.block-passed {
      opacity: 0.5;
      text-decoration: line-through;
    }
    .day-header {
      text-align: center;
      font-size: 0.8rem;
      font-weight: bold;
      color: #6c757d;
      padding: 4px 0;
    }
    .calendar-wrapper {
      max-width: 100%;
      margin: 0 auto;
    }
    @media (max-width: 576px) {
      .calendar-grid .day-cell { font-size: 0.7rem; }
      .block-btn { min-width: 60px; font-size: 0.8rem; }
    }
    .spinner-border-sm { width: 1rem; height: 1rem; }
    .loading-text { font-size: 0.9rem; color: #6c757d; }
  </style>
</head>
<body>
  <div class="container">
    <div class="card shadow p-4">
      <h2 class="text-center mb-4">📅 Agendar reunión</h2>
      <p class="text-muted text-center">Selecciona un día y luego elige un bloque horario disponible.</p>

      <% if (error) { %>
        <div class="alert alert-danger"><%= error %></div>
      <% } %>

      <!-- ========== CALENDARIO MENSUAL ========== -->
      <div class="calendar-wrapper">
        <div class="d-flex justify-content-between align-items-center mb-2">
          <span class="month-nav" id="prevMonth">‹</span>
          <h5 class="m-0" id="currentMonthLabel"></h5>
          <span class="month-nav" id="nextMonth">›</span>
        </div>

        <div class="day-header-grid" style="display: grid; grid-template-columns: repeat(7, 1fr); gap: 4px; margin-bottom: 4px;">
          <div class="day-header">Lu</div>
          <div class="day-header">Ma</div>
          <div class="day-header">Mi</div>
          <div class="day-header">Ju</div>
          <div class="day-header">Vi</div>
          <div class="day-header">Sá</div>
          <div class="day-header">Do</div>
        </div>

        <div id="calendarGrid" class="calendar-grid"></div>
      </div>

      <!-- ========== BLOQUES DISPONIBLES (cargados vía AJAX) ========== -->
      <div id="blocksContainer" style="display:none;">
        <hr />
        <h6>Bloques disponibles para el <span id="selectedDateLabel"></span></h6>
        <div id="blocksList" class="d-flex flex-wrap justify-content-center">
          <div class="loading-text">Cargando bloques...</div>
        </div>
      </div>

      <!-- ========== FORMULARIO ========== -->
      <form method="POST" action="" id="inviteForm">
        <input type="hidden" name="date" id="selectedDateInput">
        <input type="hidden" name="blockId" id="selectedBlockInput">

        <div class="mb-3">
          <label for="title" class="form-label">Título *</label>
          <input type="text" class="form-control" id="title" name="title" required
                 value="<%= locals.eventData ? eventData.title : '' %>">
        </div>

        <div class="mb-3">
          <label for="description" class="form-label">Descripción</label>
          <textarea class="form-control" id="description" name="description" rows="2"><%= locals.eventData ? eventData.description : '' %></textarea>
        </div>

        <button type="submit" class="btn btn-primary w-100" id="submitBtn" disabled>Enviar solicitud</button>
      </form>

      <p class="mt-3 text-center text-muted small">Este enlace caduca en 24 horas.</p>
    </div>
  </div>

  <script>
    // Datos del servidor
    const token = '<%= token %>';
    const meetingDuration = <%= meetingDuration %>;
    const timeFormat = '24';

    // Estado
    let currentDate = new Date();
    let selectedDate = null;
    let selectedBlockId = null;
    let currentBlocks = [];

    // Referencias DOM
    const calendarGrid = document.getElementById('calendarGrid');
    const currentMonthLabel = document.getElementById('currentMonthLabel');
    const blocksContainer = document.getElementById('blocksContainer');
    const blocksList = document.getElementById('blocksList');
    const selectedDateLabel = document.getElementById('selectedDateLabel');
    const selectedDateInput = document.getElementById('selectedDateInput');
    const selectedBlockInput = document.getElementById('selectedBlockInput');
    const submitBtn = document.getElementById('submitBtn');

    // ========== FUNCIONES ==========
    // Devuelve YYYY-MM-DD usando componentes locales (NO usa toISOString)
    function formatDateLocal(date) {
      const y = date.getFullYear();
      const m = String(date.getMonth() + 1).padStart(2, '0');
      const d = String(date.getDate()).padStart(2, '0');
      return `${y}-${m}-${d}`;
    }

    // Bloques de un día específico (usado al hacer clic en un día)
    async function fetchAvailableBlocks(dateStr) {
      try {
        const response = await fetch(`/invite/availability/${token}/${dateStr}`);
        if (!response.ok) throw new Error('Error al cargar disponibilidad');
        const data = await response.json();
        return data.blocks || [];
      } catch (error) {
        console.error('Error fetching blocks:', error);
        return [];
      }
    }

    // Disponibilidad del mes completo (una sola petición)
    async function fetchMonthAvailability(year, month) {
      try {
        const res = await fetch(`/invite/availability-month/${token}/${year}/${month}`);
        if (!res.ok) throw new Error('Error al cargar disponibilidad del mes');
        const data = await res.json();
        return data.availableDates || {};
      } catch (error) {
        console.error('Error fetching month availability:', error);
        return {};
      }
    }

    async function renderCalendar(year, month) {
      // Placeholder inmediato mientras carga
      calendarGrid.innerHTML = '<div class="text-center py-3 text-muted small">Cargando calendario...</div>';
      currentMonthLabel.textContent = new Date(year, month, 1).toLocaleString('es', { month: 'long', year: 'numeric' });

      const firstDay = new Date(year, month, 1);
      const lastDay = new Date(year, month + 1, 0);
      const daysInMonth = lastDay.getDate();
      const startDayOfWeek = firstDay.getDay();
      const startOffset = startDayOfWeek === 0 ? 6 : startDayOfWeek - 1;

      const today = new Date();
      const todayMidnight = new Date(today.getFullYear(), today.getMonth(), today.getDate(), 0, 0, 0);

      // UNA SOLA PETICIÓN para todo el mes
      const availableDates = await fetchMonthAvailability(year, month);

      let cells = [];
      for (let i = 0; i < startOffset; i++) cells.push({ empty: true });

      for (let day = 1; day <= daysInMonth; day++) {
        const dateObj = new Date(year, month, day);
        const dateStr = formatDateLocal(dateObj);
        const isPast = dateObj < todayMidnight;
        const hasAvail = availableDates[dateStr] !== undefined;

        let classes = 'day-cell';
        if (isPast || !hasAvail) classes += ' disabled';
        else classes += ' has-availability';
        if (selectedDate && dateStr === selectedDate) classes += ' selected';

        cells.push({ day, dateStr, classes });
      }

      let html = '';
      cells.forEach(cell => {
        if (cell.empty) {
          html += `<div class="day-cell disabled" style="background:transparent;"></div>`;
        } else {
          html += `<div class="${cell.classes}" data-date="${cell.dateStr}" data-day="${cell.day}">${cell.day}</div>`;
        }
      });

      calendarGrid.innerHTML = html;

      document.querySelectorAll('.day-cell:not(.disabled)').forEach(el => {
        el.addEventListener('click', function() {
          const dateStr = this.dataset.date;
          if (dateStr) {
            selectedDate = dateStr;
            selectedDateInput.value = dateStr;
            selectedBlockId = null;
            selectedBlockInput.value = '';
            submitBtn.disabled = true;

            // Actualizar solo la clase 'selected' sin re-renderizar el calendario completo
            document.querySelectorAll('.day-cell.selected').forEach(c => c.classList.remove('selected'));
            this.classList.add('selected');

            showBlocksForDate(dateStr);
          }
        });
      });
    }

    async function showBlocksForDate(dateStr) {
      blocksContainer.style.display = 'block';
      blocksList.innerHTML = '<div class="loading-text">Cargando bloques disponibles...</div>';
      selectedDateLabel.textContent = new Date(dateStr + 'T00:00:00').toLocaleDateString('es', { weekday: 'long', day: 'numeric', month: 'long' });

      const blocks = await fetchAvailableBlocks(dateStr);
      currentBlocks = blocks;

      if (blocks.length === 0) {
        blocksList.innerHTML = '<p class="text-muted">No hay bloques disponibles para esta fecha.</p>';
        return;
      }

      blocks.sort((a, b) => a.start_minutes - b.start_minutes);

      const today = new Date();
      const todayStr = formatDateLocal(today);
      const currentMinutes = today.getHours() * 60 + today.getMinutes();

      let html = '';
      blocks.forEach(block => {
        const startTime = formatTime(block.start_minutes);
        const endTime = formatTime(block.end_minutes);
        const isPast = (dateStr === todayStr) && (block.end_minutes <= currentMinutes);
        const canFit = !isPast;
        const isSelected = selectedBlockId === block.id;
        const disabled = isPast || !canFit;
        let btnClass = 'block-btn btn btn-sm ';
        if (isSelected) {
          btnClass += 'btn-primary selected';
        } else if (disabled) {
          btnClass += 'btn-secondary block-passed';
        } else {
          btnClass += 'btn-outline-primary';
        }

        html += `
          <button class="${btnClass}" data-block-id="${block.id}" ${disabled ? 'disabled' : ''}>
            ${startTime} - ${endTime}
          </button>
        `;
      });

      blocksList.innerHTML = html;

      document.querySelectorAll('.block-btn:not(:disabled)').forEach(btn => {
        btn.addEventListener('click', function() {
          const blockId = parseInt(this.dataset.blockId);
          if (blockId) {
            selectedBlockId = blockId;
            selectedBlockInput.value = blockId;
            submitBtn.disabled = false;
            document.querySelectorAll('.block-btn').forEach(b => b.classList.remove('selected', 'btn-primary'));
            this.classList.add('selected', 'btn-primary');
          }
        });
      });
    }

    function formatTime(minutes) {
      const h = Math.floor(minutes / 60);
      const m = minutes % 60;
      if (timeFormat === '12') {
        const ampm = h >= 12 ? 'PM' : 'AM';
        const h12 = h % 12 || 12;
        return `${String(h12).padStart(2, '0')}:${String(m).padStart(2, '0')} ${ampm}`;
      }
      return `${String(h).padStart(2, '0')}:${String(m).padStart(2, '0')}`;
    }

    // ========== NAVEGACIÓN ==========
    document.getElementById('prevMonth').addEventListener('click', function() {
      currentDate.setMonth(currentDate.getMonth() - 1);
      renderCalendar(currentDate.getFullYear(), currentDate.getMonth());
    });
    document.getElementById('nextMonth').addEventListener('click', function() {
      currentDate.setMonth(currentDate.getMonth() + 1);
      renderCalendar(currentDate.getFullYear(), currentDate.getMonth());
    });

    // ========== VALIDACIÓN AL ENVIAR ==========
    document.getElementById('inviteForm').addEventListener('submit', function(e) {
      if (!selectedDate || !selectedBlockId) {
        alert('Por favor, selecciona un día y un bloque horario.');
        e.preventDefault();
        return;
      }
    });

    // ========== INICIALIZACIÓN ==========
    renderCalendar(currentDate.getFullYear(), currentDate.getMonth());

    // Restaurar selección previa si hay error
    <% if (locals.eventData && eventData.date && eventData.blockId) { %>
      const prevDate = '<%= eventData.date %>';
      const prevBlockId = parseInt('<%= eventData.blockId %>');
      selectedDate = prevDate;
      selectedDateInput.value = prevDate;
      selectedBlockId = prevBlockId;
      selectedBlockInput.value = prevBlockId;
      submitBtn.disabled = false;
      renderCalendar(currentDate.getFullYear(), currentDate.getMonth()).then(() => {
        showBlocksForDate(prevDate).then(() => {
          setTimeout(() => {
            document.querySelectorAll('.block-btn').forEach(btn => {
              if (parseInt(btn.dataset.blockId) === prevBlockId) {
                btn.click();
              }
            });
          }, 100);
        });
      });
    <% } %>
  </script>
</body>
</html>
