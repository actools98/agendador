<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Mi Calendario</title>
  <!-- Bootstrap 5 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>

<div class="container-fluid p-0 vh-100">

  <!-- ========== CABECERA SUPERIOR (siempre visible) ========== -->
  <div class="mobile-header d-flex align-items-center justify-content-between px-3 py-2">
    <div class="d-flex align-items-center gap-2">
      <button id="menuToggle" class="btn btn-outline-secondary btn-sm" aria-label="Menú">
        ☰
      </button>
      <h5 class="m-0" id="headerTitle">📅 Calendario</h5>
    </div>
    <div class="d-flex gap-1">
      <button id="newEventBtn" class="btn btn-sm btn-primary" title="Nuevo evento">+</button>
    </div>
  </div>

  <!-- ========== CONTENIDO PRINCIPAL ========== -->
  <div class="main-content" style="height: calc(100vh - 56px); overflow: hidden;">

    <!-- ===== VISTA 1: CALENDARIO (visible por defecto) ===== -->
    <div id="viewCalendar" class="view-panel h-100 overflow-auto p-3">
      <!-- Controles de navegación -->
      <div class="d-flex flex-wrap align-items-center justify-content-between mb-3">
        <div class="d-flex align-items-center gap-2">
          <button id="prevView" class="btn btn-secondary btn-sm">‹</button>
          <button id="nextView" class="btn btn-secondary btn-sm">›</button>
          <span id="viewTitle" class="ms-1 h5 mb-0"></span>
        </div>
        <div class="btn-group btn-group-sm" role="group" aria-label="Vistas">
          <button class="btn btn-outline-primary view-btn" data-view="day">Día</button>
          <button class="btn btn-outline-primary view-btn" data-view="week">Semana</button>
          <button class="btn btn-outline-primary view-btn active" data-view="month">Mes</button>
          <button class="btn btn-outline-primary view-btn" data-view="year">Año</button>
        </div>
      </div>

      <!-- Grid del calendario -->
      <div id="calendarGrid" class="calendar-grid"></div>

      <!-- Footer -->
      <div id="debug-message" class="debug-message">Powered by actols.com</div>
    </div>

    <!-- ===== VISTA 2: GESTIÓN (sidebar) ===== -->
    <div id="viewManagement" class="view-panel h-100 overflow-auto p-3" style="display:none; background-color: var(--sidebar-bg);">
      <div class="sidebar-content">
        <div class="d-flex align-items-center justify-content-between mb-3">
          <h5 class="m-0">📋 Gestión</h5>
          <button id="closeManagement" class="btn btn-sm btn-outline-secondary">✕ Cerrar</button>
        </div>

        <div class="mb-3">
          <p class="text-muted small">Bienvenido, <strong><%= username %></strong></p>
        </div>

        <hr class="border-secondary" />

        <!-- Botones de acción -->
        <div class="d-grid gap-2 mb-3">
          <button id="newEventBtnSidebar" class="btn btn-primary">+ Nuevo evento</button>
          <button id="toggleFinishedBtnSidebar" class="btn btn-outline-secondary">📋 Eventos terminados</button>
        </div>

        <!-- Lista de eventos terminados -->
        <div id="finishedEventsList" class="list-group list-group-flush mt-2" style="display:none;"></div>

        <!-- Eventos activos -->
        <h6 class="mt-3">Mis eventos activos</h6>
        <div id="eventList" class="list-group list-group-flush mt-2"></div>

        <hr class="border-secondary" />

        <!-- Botones de gestión -->
        <button id="categoriasBtnSidebar" class="btn btn-outline-secondary w-100 mb-2">🏷️ Categorías</button>
        <button id="settingsBtnSidebar" class="btn btn-outline-secondary w-100 mb-3">⚙️ Configuración</button>
        <a href="/logout" class="btn btn-outline-danger w-100">Cerrar sesión</a>
      </div>
    </div>

  </div>
</div>

<!-- ========== MODALES (sin cambios) ========== -->
<!-- MODAL PARA EVENTOS -->
<div class="modal fade" id="eventModal" tabindex="-1">
  <div class="modal-dialog modal-dialog-centered modal-sm">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="modalTitle">Evento</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
      </div>
      <div class="modal-body">
        <form id="eventForm">
          <input type="hidden" id="eventId" />
          <div class="mb-2">
            <label for="title" class="form-label">Título *</label>
            <input type="text" class="form-control form-control-sm" id="title" required />
          </div>
          <div class="mb-2">
            <label for="description" class="form-label">Descripción</label>
            <textarea class="form-control form-control-sm" id="description" rows="2"></textarea>
          </div>
          <div class="row g-2">
            <div class="col-6">
              <label for="start" class="form-label small">Inicio *</label>
              <input type="datetime-local" class="form-control form-control-sm" id="start" required />
            </div>
            <div class="col-6">
              <label for="end" class="form-label small">Fin *</label>
              <input type="datetime-local" class="form-control form-control-sm" id="end" required />
            </div>
          </div>
          <div class="mb-2 form-check">
            <input type="checkbox" class="form-check-input" id="allDay" />
            <label class="form-check-label" for="allDay">Todo el día</label>
          </div>
          <div class="mb-2">
            <label for="eventCategoria" class="form-label small">Categoría</label>
            <select id="eventCategoria" class="form-select form-select-sm">
              <option value="">Sin categoría</option>
            </select>
          </div>
          <div class="mb-2">
            <label for="color" class="form-label small">Color (evento)</label>
            <input type="color" class="form-control form-control-color form-control-sm" id="color" value="#3788d8" />
          </div>
          <div class="mb-2">
            <label for="eventStatus" class="form-label small">Estado</label>
            <select id="eventStatus" class="form-select form-select-sm">
              <option value="active">Activo</option>
              <option value="completed">Completado</option>
              <option value="postponed">Pospuesto</option>
              <option value="cancelled">Cancelado</option>
            </select>
          </div>
        </form>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-outline-secondary btn-sm" data-bs-dismiss="modal">Cancelar</button>
        <button type="button" id="deleteEventBtn" class="btn btn-danger btn-sm me-auto" style="display:none;">Eliminar</button>
        <button type="button" id="saveEventBtn" class="btn btn-primary btn-sm">Guardar</button>
      </div>
    </div>
  </div>
</div>

<!-- MODAL DE CATEGORÍAS -->
<div class="modal fade" id="categoriasModal" tabindex="-1">
  <div class="modal-dialog modal-dialog-centered modal-sm">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">🏷️ Categorías</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
      </div>
      <div class="modal-body">
        <div id="categoriasList" class="list-group list-group-flush mb-3"></div>
        <form id="categoriaForm">
          <input type="hidden" id="categoriaEditId" />
          <div class="row g-2">
            <div class="col-7">
              <input type="text" id="categoriaNombre" class="form-control form-control-sm" placeholder="Nombre" required />
            </div>
            <div class="col-3">
              <input type="color" id="categoriaColor" class="form-control form-control-color form-control-sm" value="#6c757d" />
            </div>
            <div class="col-2">
              <button type="submit" id="categoriaSaveBtn" class="btn btn-primary btn-sm w-100">Guardar</button>
            </div>
          </div>
        </form>
        <div id="categoriaError" class="text-danger small mt-1" style="display:none;"></div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary btn-sm" data-bs-dismiss="modal">Cerrar</button>
      </div>
    </div>
  </div>
</div>

<!-- MODAL DE CONFIGURACIÓN -->
<div class="modal fade" id="settingsModal" tabindex="-1">
  <div class="modal-dialog modal-dialog-centered modal-sm">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">⚙️ Configuración</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
      </div>
      <div class="modal-body">
        <div class="mb-2">
          <label for="timeFormatSelect" class="form-label small">Formato de hora</label>
          <select id="timeFormatSelect" class="form-select form-select-sm">
            <option value="24">24h (00:00 – 23:59)</option>
            <option value="12">12h (AM/PM)</option>
          </select>
        </div>
        <div class="mb-2">
          <label for="themeSelect" class="form-label small">Tema</label>
          <select id="themeSelect" class="form-select form-select-sm">
            <option value="claro">Claro</option>
            <option value="oscuro">Oscuro</option>
          </select>
        </div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary btn-sm" data-bs-dismiss="modal">Cerrar</button>
        <button type="button" id="saveSettingsBtn" class="btn btn-primary btn-sm">Guardar cambios</button>
      </div>
    </div>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script src="/js/app.js"></script>
</body>
</html>
